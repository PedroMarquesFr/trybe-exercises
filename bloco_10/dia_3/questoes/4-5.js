const returnUpperCaseString = (str) => str.toUpperCase();

const returnStringFirstLetter = (str) => str[0];

const concatStrings = (str1, str2) => str1 + str2;

module.exports = {
  returnUpperCaseString,
  returnStringFirstLetter,
  concatStrings,
};
