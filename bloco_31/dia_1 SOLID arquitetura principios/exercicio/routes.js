const express = require("express");

const router = express.Router();

router.get("/plants",)
router.get("/plant/:id ",)
router.delete("/plant/:id",)
router.put("/plant/:id ",)
router.post("/plant",)
router.get("/sunny/:id",)

module.exports = router;